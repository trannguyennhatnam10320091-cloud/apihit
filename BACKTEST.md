# Backtest

Chạy:

```bash
npm run backtest -- /duong/dan/hitclub-history-all-2026-08-03.csv
```

Backtest chỉ kiểm tra thuật toán có chạy đúng quy tắc trên dữ liệu cũ. Kết quả quá khứ không bảo đảm các phiên sau.
